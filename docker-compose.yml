version: "3.8"

services:
  app:
    image: node:20
    working_dir: /app
    command: sh -c "git clone https://github.com/rikigohain0028/Recoonlytics.git . && npm install && npm run build && npm run dev"

